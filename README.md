# djangocrud

## Run the server

Activate the virtual environment first, then run the server:

```bash
source venv/bin/activate
python manage.py runserver
```

Or use the helper script:

```bash
./runserver.sh
```

If port 8000 is in use, use another port: `./runserver.sh 8080` or `python manage.py runserver 8080`.
